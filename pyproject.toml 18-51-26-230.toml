[project]
name = "slogger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jprobrien", email = "98370624+89jobrien@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
slogger = "slogger:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
